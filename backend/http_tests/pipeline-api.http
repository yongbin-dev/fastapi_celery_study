@baseUrl = http://localhost:5050
@chain_id = example-chain-id-12345

### 사용 가능한 모델 조회
GET {{baseUrl}}/api/v1/tasks/models

###

### AI 예측 요청 (qwen2.5vl 모델)
POST {{baseUrl}}/api/v1/tasks/predict
Content-Type: application/json

{
  "server": "server1",
  "model": "qwen2.5vl:7b-q8_0",
  "prompt": "안녕하세요, 간단한 인사를 해주세요."
}

###

### AI 예측 요청 (qwen3 모델)
POST {{baseUrl}}/api/v1/tasks/predict
Content-Type: application/json

{
  "server": "server2",
  "model": "qwen3:32b",
  "prompt": "파이썬으로 Hello World를 출력하는 코드를 작성해주세요."
}

###

### AI 파이프라인 생성
POST {{baseUrl}}/api/v1/tasks/ai-pipeline
Content-Type: application/json

{
  "pipeline_name": "test_pipeline",
  "tasks": [
    {
      "task_name": "data_preprocessing",
      "task_type": "preprocessing",
      "parameters": {
        "input_data": "test input data",
        "preprocessing_steps": ["normalize", "clean"]
      }
    },
    {
      "task_name": "ai_inference",
      "task_type": "ai_processing",
      "parameters": {
        "model_name": "qwen2.5vl:7b-q8_0",
        "prompt": "이 데이터를 분석해주세요."
      }
    }
  ]
}

###

### 파이프라인 히스토리 조회 (기본 1시간)
GET {{baseUrl}}/api/v1/tasks/history

###

### 파이프라인 히스토리 조회 (지난 24시간)
GET {{baseUrl}}/api/v1/tasks/history?hours=24

###

### 파이프라인 히스토리 조회 (상태 필터)
GET {{baseUrl}}/api/v1/tasks/history?status=completed

###

### 파이프라인 히스토리 조회 (태스크 이름 필터)
GET {{baseUrl}}/api/v1/tasks/history?task_name=data_preprocessing

###

### 특정 파이프라인 태스크 조회
GET {{baseUrl}}/api/v1/tasks/ai-pipeline/{{chain_id}}/tasks

###

### 파이프라인 취소
DELETE {{baseUrl}}/api/v1/tasks/ai-pipeline/{{chain_id}}/cancel

###